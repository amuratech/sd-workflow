spring:
  profiles:
    active: test
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

server:
  port: 8089
  servlet:
    context-path: /

security:
  jwt:
    key: test
  crypto:
    key: secret
client:
  iam:
    basePath: http://localhost:9090/iam
  config:
    basePath: http://localhost:9090/config
  sales:
    basePath: http://localhost:9090/sales
